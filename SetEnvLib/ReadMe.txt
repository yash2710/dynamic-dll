========================================================================
    DYNAMIC LINK LIBRARY : SetEnvLib Project Overview
========================================================================

This DLL Injects the specified environment variables to the particular process. It's doing inside DLL Main function